# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Resume Crafter is a tool for creating websites for resume. The project is in early initialization stage.

## Project Setup

This is a Node.js project using CommonJS modules (`"type": "commonjs"` in package.json).

Currently there are no dependencies installed and no build/test commands configured beyond the default npm placeholder.

## Development Commands

No development commands have been configured yet. The package.json contains only a placeholder test script.

## Architecture

The codebase structure is not yet established. Source code files should be added as development progresses.

## Notes

- The `requirements` directory exists but is currently empty
- No testing framework has been configured
- No build process has been set up